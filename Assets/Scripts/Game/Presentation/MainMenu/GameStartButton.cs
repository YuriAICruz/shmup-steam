using UnityEngine;

namespace Graphene.Game.Presentation.MainMenu
{
    public class GameStartButton : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
